import Handler from '../Handler';
import Transport from '../../interfaces/Transport';

export default class Trash extends Handler implements Transport {
  /*
  constructor() {
    super();
  }
  */
  registerIpc(): void {
  }
}
