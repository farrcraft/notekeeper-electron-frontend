import Handler from '../Handler';
import Transport from '../../interfaces/Transport';

export default class Tag extends Handler implements Transport {
  /*
  constructor() {
    super();
  }
  */
  registerIpc(): void {
  }
}
